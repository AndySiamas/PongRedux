class Entity {
    constructor(name, points, width, height, origin) {
        this.name = name;
        this.points = points;
        this.width = width;
        this.height = height;
        this.origin = origin;
    }
};

export default Entity;